{
    "bigram": "Given the following traffic data of {} level that contains a sequence which is a sequence derived from the Bert Tokenizer's tokenization of the bigram of a packet's TCP payload, where a bigram refers to a byte sequence formed by concatenating adjacent 4 bytes and rolling at intervals of 2 bytes, from the original byte sequence. Please conduct the MOBILE BEHAVIOR DETECTION TASK to determine which type of mobile behavior the traffic belongs to. The categories include 'sendText, sendAudio, sendImage, shareLocationOnce, transferFile'.\\n{}: {}",
    "bigram-raw": "Given the following traffic data of {} level that contains a string which is a bigram of a packet's TCP payload, where a bigram refers to a byte sequence formed by concatenating adjacent 4 bytes and rolling at intervals of 2 bytes, from the original byte sequence. Please conduct the MOBILE BEHAVIOR DETECTION TASK to determine which type of mobile behavior the traffic belongs to. The categories include 'sendText, sendAudio, sendImage, shareLocationOnce, transferFile'.\\n{}: {}"
}